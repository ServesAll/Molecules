import {
    H3,
    Center,
    CenterLeft,
    CenterRight,
    MarginBottom,
    Margin,
    Padding,
    Row,
    TextBtn,
    RoundedBtn,
    RoundBtn,
    FloatingBtn,
    Header,
} from '@servesall/atoms';

# Header

Header component

<MarginBottom style={{minHeight:'80px'}}>
        <Header height={100} style={{backgroundColor: '#2d3436'}}>
            <Row>
                <CenterLeft>
                    <Margin>
                    <H3 color="#FFFFFF">Logo</H3>
                    </Margin>
                </CenterLeft>
                <CenterRight>
                    <Margin>
                    <H3 color="#FFFFFF">account</H3>
                    </Margin>
                </CenterRight>
            </Row>
        </Header>
</MarginBottom><br />


```code
import {
    H3,
    Margin,
    Header,
    CenterLeft,
    useThemeContext,
} from '@servesall/atoms';

function RootElement() {
    const {themeContext} = useThemeContext();
    const primaryColor = themeContext.color1;
    const secondaryColor = themeContext.color2;

  return (
   <Header height={100} style={{backgroundColor: primaryColor}}>
            <Row>
                <CenterLeft>
                    <Margin>
                    <H3 color={secondaryColor}>Logo</H3>
                    </Margin>
                </CenterLeft>
                <CenterRight>
                    <Margin>
                    <H3 color={secondaryColor}>account</H3>
                    </Margin>
                </CenterRight>
            </Row>
   </Header>
  );
}
```

## Props

<table>
    <thead>
    <tr>
        <th>Prop</th>
        <th>type</th>
        <th>default</th>
        <th>required</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td>style</td>
        <td>object</td>
        <td>No</td>
        <td>No</td>
    </tr>
    <tr>
        <td>height</td>
        <td>string</td>
        <td>none</td>
        <td>Yes</td>
    </tr>
    <tr>
        <td>StatusBarOptions</td>
        <td>object</td>
        <td>
            hidden: false,
            barStyle: 'dark-content',
            backgroundColor: '#FFFFFF',
            translucent: false
        </td>
        <td>No</td>
    </tr>
    </tbody>
</table>
